<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Order Confirmation</title>
    <style>
      body { font-family: 'Poppins', Arial, sans-serif; margin: 0; padding: 0;
      background-color: #f5f8fc; color: #333; text-align: center; font-size:
      18px; } .container { max-width: 600px; margin: 20px auto;
      background-color: #ffffff; border-radius: 8px; box-shadow: 0 4px 6px
      rgba(0, 0, 0, 0.1); overflow: hidden; } .header { background-color:
      #eef4ff; padding: 20px; } .header h1 { font-size: 28px; margin: 0; color:
      #2a61ee; } .icon { margin: 20px auto; width: 50px; } .content { padding:
      20px; } .content h3 { margin-bottom: 10px; font-size: 22px; color: #555; }
      .order-summary { text-align: left; margin: 10px 0; border: 1px solid
      #dfe4ea; border-radius: 8px; padding: 15px; background-color: #f9fbfd;
      font-size: 20px; } .order-summary table { width: 100%; border-collapse:
      collapse; } .order-summary th, .order-summary td { border: 1px solid
      #dfe4ea; padding: 10px; text-align: left; } .order-summary th {
      background-color: #eef4ff; font-weight: bold; } .total { margin: 20px 0;
      font-size: 20px; font-weight: bold; color: #2a61ee; } .shipping-address,
      .paragraph { text-align: left; margin: 20px 0; font-size: 18px; } .footer
      { background-color: #2a61ee; color: white; padding: 15px; } .footer a {
      color: white; text-decoration: none; } .footer a:hover { text-decoration:
      underline; } .contact { margin-top: 10px; font-size: 16px; font-size:
      20px; font-weight: bold;} table tr:first-child th:first-child {
      border-top-left-radius: 0.5rem; }
    </style>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Thank you for your order, {{user}}!</h1>
      </div>
      <div class="content">
        <p class="paragraph">Your order has been successfully placed. Below are
          the details:</p>

        <h3>Order Summary:</h3>
        <div class="order-summary">
          <table>
            <thead>
              <tr>
                <th>Product Name</th>
                <th>Quantity</th>
              </tr>
            </thead>
            <tbody>
              {{#each orderItems}}
                <tr>
                  <td>{{this.product}}</td>
                  <td>{{this.quantity}}</td>
                </tr>
              {{/each}}
            </tbody>
          </table>
        </div>

        <p class="total">Total Cost: ${{totalCost}}</p>

        {{#if shippingAddress}}
          <div class="shipping-address">
            <h3>Shipping Address:</h3>
            <p>
              {{shippingAddress.address}},<br />
              {{shippingAddress.city}},
              {{shippingAddress.postalCode}},<br />
              {{shippingAddress.country}}
            </p>
          </div>
        {{else}}
          <div>
            <p class="paragraph">This is a pickup order. Please collect it on
              {{pickUpDate}}.</p>
          </div>
        {{/if}}

        <p class="paragraph">Thank you for choosing us!</p>
      </div>
      <div class="footer">
        <p class="paragraph">Problems with the Order?
          <a
            href="mailto:cheesecakesbybattle@gmail.com"
            class="paragraph"
          >cheesecakesbybattle@gmail.com</a></p>
        <div class="contact">Cheese Cakes By Battle</div>
      </div>
    </div>
  </body>
</html>